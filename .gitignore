# Source videos: ignore ALL by default (fail-safe). The published site only needs
# the short-form sources (IG / TikTok / YouTube Shorts) that the lightbox plays as
# local mp4s; long-form YouTube sources are huge and only seed hover previews
# locally (the live site uses the YT embed).
#
# This is a pattern, NOT a per-file denylist, on purpose: the old denylist named 17
# long-form files one by one, so any NEW long-form pull was committable by default.
# On 2026-08-21 that had grown to 516 MB of untracked long-form sitting one
# `git add -A` away from blowing the 1 GB GitHub Pages limit (501 MB -> 1238 MB).
#
# Already-tracked sources stay tracked (gitignore never untracks), and the editor's
# publish sweep force-adds the assets projects.js actually references, so curated
# short-form still ships. See _handle_publish in scripts/editor-server.py.
videos/source/*.mp4
videos/source/*.part

# Cookies files (sensitive)
cookies.txt
tracker/tiktok/cookies.txt
tracker/instagram/cookies.txt

# Environment variables (sensitive)
.env
tracker/.env

# Google Service Account credentials (sensitive)
_key/

# Python
__pycache__/
*.pyc
.venv/
venv/
tracker/venv/
tracker/instagram/debug_output.txt

# OS generated files
.DS_Store
Thumbs.db

# Windows reserved filenames (accidentally created)
nul

# IDE files
.idea/
.vscode/
*.swp
*.swo

# Resume and cover letter (personal)
Daniel_Aparecido_Resume.txt
Daniel_Aparecido_Cover_Letter.txt

# Claude Code memory files
CLAUDE.md

# Node modules (if any)
node_modules/

# Local-only tooling (not needed for the published site or CI; keep private)
editor.html
electron/
scripts/editor-server.py
scripts/editor-app.bat
scripts/run-editor.bat
scripts/process_previews.py
scripts/yt_hls_manifest.py
scripts/fetch_thumbnails.py
scripts/process-videos.ps1
scripts/process-videos.bat
scripts/sync-videos.bat
scripts/update-portfolio.bat
scripts/refresh-cookies.bat
scripts/refresh-cookies.ps1
scripts/inspect_sheets.py

# Working files / drafts
draft.psd
.tmp/

# Confidential client reference (micro1 / Titan Obsidian NDA) - NEVER commit or deploy
Event Detection Cheat Sheet.html

# Personal documents and stray root files (deploy.yml publishes the whole repo root,
# so anything tracked here is served at a guessable URL on daniaparecido.com)
Daniel Aparecido - Video Editor CV.pdf
dani logo.png
